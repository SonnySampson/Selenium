using System;
using OpenQA.Selenium;
using System.Collections.Generic;

namespace SeleniumExample.PageObjects
{
    /// <summary>
    /// Primarily is used to store the default items that each elmentMap class needs. 
    /// Each elementMap class is used to store a specific group of objects that are interacted with.
    /// It is required for generics of the IPageObject and IValidator interfaces.
    /// </summary>
    public interface IElementMap
    {
        
    }
}
